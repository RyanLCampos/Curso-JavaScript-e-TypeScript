
// Dados Primitivos
// String, number, undefined, null, boolean, symbol

// STRINGS
const nome = 'Ryan';
const nome2 = "Ryan";
const nome3 = `Ryan`;

// NUMBER
const num = 10;
const num2 = 10.52;

let nomeAluno; // undefined -> Não aponta para local nenhuma da memoria
const sobrenomeAluno = null; // Nulo -> não aponta para local nenhum da memoria

// Boolean (true or false) (Lógico)
const aprovado = true;
const aprovado2 = false;